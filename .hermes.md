# traffic-video-agent — 项目工作规范

> 继承全局关键规则（见 ~/.hermes.md）。本文件聚焦本项目特有约束。

## 关键原则（继承自全局）

- **主动推送，不要等待追问**——定时任务完成后主动推送结果
- **系统问题优先处理**——Gateway 崩了要先恢复，再做其他事
- **效率优先**——能自动化的不手动，能并行的不串行

## 项目定位（来源：2026-07-29 飞书会议纪要）

引流传单号矩阵业务——基于课程 Agent 底层能力，独立开发批量短视频生成模块。

- 单条视频 2-10 分钟，无出镜操作演示形式
- 内容方向：行业热点解读、基础操作教学
- 不依赖课程大纲，从零散素材自动拆解知识点
- 目标：账号矩阵指数级扩张，产能提升 10-100 倍

## 技术选型（来源：2026-07-29 飞书会议纪要，多模型混用策略）

- 长视频解析：Gemini
- 专业内容/长任务处理：DeepSeek
- 禁用：豆包、MiniMax（专业场景内容虚构问题）
- 选型原则：优先经过大规模验证的稳定模型，不用未测试新模型
- 成本参考：48 集 B 站长视频解析约 5 元

## Hindsight 记忆协议（继承自全局，精简版）

每次完成基础设施操作，必须 retain 一条到 Hindsight：

| 操作类型 | scope tag |
|---|---|
| Docker 容器增删/重启 | `scope:docker` |
| systemd unit 修改/启停 | `scope:systemd` |
| 反代/隧道/端口转发变更 | `scope:network` |
| cron/定时任务增删 | `scope:cron` |
| 数据库 schema/迁移 | `scope:db` |
| 服务端口/监听 socket 变更 | `scope:ports` |

格式：`YYYY-MM-DD HH:MM <操作类型>: <一句话> | 容器/服务: <name> | 端口/路径: <port/path> | 状态: <up/down/changed>`

调用方式：`from hindsight_helper import HindsightHelper; h = HindsightHelper(agent="main")`

## 凭印象病黑名单

写任何具体值前必先 query 验证（grep/cat/read_file/curl/skill_view），写完立即回读 grep 验证。用任何工具/CLI/API 前先 `--help` 或查官方文档。详见 SOUL.md 铁律 6+7 及 ~/.hermes.md 凭印象病黑名单段。

## Git 管理

- Gitee 主仓（日常直连 push）：`origin` → https://gitee.com/luo-xiansen2023/traffic-video-agent
- GitHub 镜像（走 socks5 代理）：`github` → https://github.com/leonluo2008-ops/traffic-video-agent
- GitHub push 前需：`git config http.https://github.com.proxy "socks5://127.0.0.1:7897"`
- 禁 `git add -A`

## 目录结构

```
traffic-video-agent/
├── README.md
├── .hermes.md          # 本文件
├── .gitignore
├── src/                # 源码（待开发）
├── projects/           # 产出数据（gitignore）
└── output/             # 视频输出（gitignore）
```
